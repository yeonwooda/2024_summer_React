const Red = () => {
  const redStyle = {
    width: "100px",
    height: "100px",
    borderRadius: "9999px",
    backgroundColor: "red",
  };

  return <div style={redStyle}></div>;
};

export default Red;
